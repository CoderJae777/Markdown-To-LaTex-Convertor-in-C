# Complex Demo

This paragraph mixes **bold**, *italic*, ***bold italic***, `inline code`, and $a^2 + b^2 = c^2$.

- parent item
  - nested child
  - nested child with **bold**
- parent item with code block:
  ```
  for (int i = 0; i < 3; i++) {
      printf("hi\n");
  }
  ```

> Blockquote text with a [link](https://openai.com)
> and more inline formatting like `code`.

$$
E = mc^2
$$
